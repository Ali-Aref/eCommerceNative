import { LogBox } from 'react-native';

const LogBoxUpdated = [
  LogBox.ignoreLogs(['AxiosError: Request failed with status code 422']), // Ignore log notification by message,
  
]

export default LogBoxUpdated;
